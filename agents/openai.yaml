interface:
  display_name: "GitHub 安全发布"
  short_description: "所有 GitHub 上传前统一审计、脱敏并执行门禁"
  default_prompt: "Use $github-safe-publish before any GitHub push, upload, sync, open-source, or Release action, and stop unless the exact publication copy receives pass."

policy:
  allow_implicit_invocation: true
