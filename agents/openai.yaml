interface:
  display_name: "GitHub 安全发布指导"
  short_description: "指导 Codex 按授权安全完成 GitHub 发布"
  default_prompt: "Use $github-safe-publish to complete only the GitHub write types the user names, after proportionate review and checks, and stop when the specified remote result is read back."

policy:
  allow_implicit_invocation: true
