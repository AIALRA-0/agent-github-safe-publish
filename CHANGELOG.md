# 变更记录

本项目使用语义化版本；稳定版本冻结后，只在安全、兼容、恢复或可复现错误放行与错误阻断事件中继续维护

## 1. 2.0.1 — 2026-09-02

- 收紧隐式触发，只把 GitHub 仓库或 Git 远端语境中的发布请求交给 Skill
- 增加逐对象写入授权、push-only 负向边界、保护规则与管理员绕过区分、不可信内容边界、远端并发复审和 Tag、Release、附件幂等重试指导
- 明确严格门禁只有在可验证缺陷或维修状态下才能降级为实际传输面的五类轻量关键审核
- 增加安装与第一次成功路径的中英文 README，并把默认 Skill、可选 CLI 和旧兼容入口的产品身份分开
- 增加 16 个合成候选场景和开发期独立 Agent 行为评测接口，不把候选评测伪装成运行时 Gate

## 2. 2.0.0 — 2026-09-01

- 产品入口收敛为 Codex GitHub 安全发布指导 Skill，不再把独立 Gate、Policy、签名认证或 Docker 当成普通发布前置条件
- 默认流程改为查明真实 Git 发布面、直接修复风险、运行项目原生检查、普通快进推送、跟进 CI，并完成授权的 Tag 或 Release
- Docker 不再是 Skill 依赖；加载 Skill 不得启动、安装、修复或等待 Docker
- Python v2 工具稳定为 `2.0.0` 并保留为可选兼容能力；旧入口版本继续返回 `1.1.7`
- 复用 GitHub Actions 工作流改为始终 advisory，不能阻断调用方工作流
- 已有严格门禁处于缺陷修复期时，默认改用凭据、私人身份与数据、内部基础设施和受保护资产的轻覆盖关键审核
- 保留候选确定性、Windows 长路径和已提交 Git Blob 收据修复，避免可选工具在跨平台候选中产生错误差异

## 3. 2.0.0-rc.2 — 2026-08-31

- 把 Gitleaks 返回的绝对候选路径规范化为仓库相对路径，避免私有候选根进入 Finding 和报告
- 把 Policy v4 的精确保留证据接入 Gitleaks；只有对象摘要、规则、工具、策略与期限全部匹配时才保留已证明公开的内容
- 增加绝对路径、精确保留和内容变化失效攻击回归；ReadWeave 候选的 7 条公开占位符误报由 5 个精确对象证据收敛为 0 条未解决 Finding
- 根据完整 Pytest 摘要计算，本轮回归共 $160=159+1$ 项；159 项通过、失败 0 项、1 项实时容器金丝雀因隔离后端不可用而跳过

## 4. 2.0.0-rc.1 — 2026-08-31

- 候选认证新增 Commit、Tree、Index、Patch、逐对象覆盖和完整授权收据绑定
- Windows Ed25519 私钥改用当前用户的数据保护接口；旧明文密钥在读取时原位迁移
- 可信发布器不再读取源仓库和私有策略，并拒绝未授权的 GitHub Actions 工作流写入
- 固定 Gitleaks 8.30.1 可执行文件摘要，运行凭据金丝雀，并把全遮蔽候选扫描纳入认证
- 项目功能命令删除宿主机回退，只能在固定摘要、非 Root、无网络和只读挂载的容器中执行
- 增加精确源审计收据、Policy v1 至 v3 内存迁移、限时保留证据和路径穿越防护
- 图片、PDF、Office、ZIP、字体和 WASM 增加资源上限、元数据处理与安全剔除回退
- FontTools 升级到 4.64.0，关闭 4.60.2 之前版本中的任意文件写入与 XML 注入漏洞
- 候选构建失败、网络失败、所有者决策和法律冻结均保留证据并支持对应恢复边界
- 根据完整 Pytest 摘要计算，Windows 本地回归共 $159=158+1$ 项；158 项通过、失败 0 项、1 项实时容器金丝雀因隔离后端不可用而跳过，因此 RC 保留真实项目认证边界，不把该项写成通过

## 5. 2.0.0-beta.1 — 2026-08-31

- 新增无网络、只读根文件系统、只读候选、去除全部 Linux capabilities 和资源上限的容器验证
- Windows 使用独立 WSL Docker Engine；固定镜像摘要并禁止运行时自动拉取
- 使用 Ed25519 签署 Candidate Tree、策略、工具、验证、降级和远端目标绑定
- 可信发布器使用预先固定公钥指纹，并核对授权到期时间、写入类型、降级上限、远端 Base、Commit 和 Tree
- 分离 `sanitize`、`verify`、`publish` 和 `resume`，相同绑定检查点支持幂等恢复
- 增加有界收敛循环、LFS Pointer 与 Submodule 安全剔除、法律保护区和大对象流式摘要
- 私有发布证明采用 in-toto Statement 与 SLSA Verification Summary 结构
- 锁定 `cryptography==50.0.1`，保证 Linux CI 与本机均能加载 Ed25519 签名模块
- 根据完整 Pytest 摘要计算，Beta 1 回归共 $134=134+0$ 项；134 项通过、失败 0 项，其中包含 1 项真实 WSL Docker 隔离金丝雀

## 6. 2.0.0-alpha.2 — 2026-08-31

- SQLite 只保留 Schema 并删除全部真实行
- Notebook 清空输出、执行计数和元数据，再替换输入中的私有实体
- ZIP 使用路径、成员数和展开体积限制递归清洗文本成员，并以固定时间戳重新打包
- 未支持制品只有在策略声明为可选时才自动剔除并生成说明，否则进入 `needs_input`

## 7. 2.0.0-alpha.1 — 2026-08-31

- 新增 Policy v4、源快照、发现、修复动作、候选清单、验证结果、认证、授权与发布证明模型
- 新项目公开默认创建安全新根，不复制私有 Git 历史
- 实现文本凭据外置、私人实体稳定替换、私有 IP 参数化和 `.env` 示例生成
- 功能验证移除发布凭据并禁止 Python 字节码修改候选
- 新增本机裸 Git 可信发布路径，远端 Commit 与 Tree 必须等于认证对象
- 第一条 North Star 测试证明源项目零修改、旧历史不可达、候选无植入值并最终进入 `published`
- v1 命令继续通过兼容入口使用；暴露面调查不进入 v2 单项目发布判定

## 8. 1.1.7 — 2026-08-31

- 关闭旧 `managed-publish` 自动合并，只保留审计和创建待复核 Pull Request
- 把占位符识别收紧为精确语法，测试路径不再降低未标记凭据的风险
- 保留同一 Git Blob 的全部历史路径语境，并为大对象增加有界流式读取
- 为 GitHub Pages 增加协议、端口、解析地址、重定向目标和连接地址检查
- 每次使用 Gitleaks 缓存时重新核对固定官方归档摘要和本机可执行文件收据
- 项目验证移除发布、SSH 和云端凭据，并拒绝验证期间变化的候选
- 私有正则拒绝嵌套量词、反向引用和高风险断言；私有目录权限失败会立即停止
- 允许位置只有在对象、扫描器、策略、签发时间和到期时间全部匹配时才能生效
- 冻结 v2 产品合同、威胁模型、模块架构和迁移边界

## 9. 1.1.6 — 2026-08-30

- 把可解码文本的完整模式扫描上限与 25 MiB 单文件安全上限统一，锁文件和源映射不再因超过 1 MiB 留下覆盖缺口
- 增加秘密位于第 1 MiB 之后的回归测试，超过 25 MiB 的对象继续按覆盖不完整处理

## 10. 1.1.5 — 2026-08-30

- 允许精确对象路径包含字面方括号；批准、例外和风险接受仍使用字符串相等比较，`*` 与 `?` 继续禁止
- 增加方括号路径与真实通配符的对照测试，修复合法文档文件名造成的错误阻断

## 11. 1.1.4 — 2026-08-30

- 允许信息所有者按对象与 SHA-256 摘要精确批准已通过独立解析器复核的异常 ZIP；内容变化后批准自动失效
- 未批准的异常 ZIP 继续按覆盖不完整处理，并增加批准、未批准和摘要变化三组回归测试

## 12. 1.1.3 — 2026-08-30

- 允许信息所有者按对象与 SHA-256 摘要精确批准已人工检查的异常 SVG；内容变化后批准自动失效
- 增加异常 SVG 批准与摘要变化失效的回归测试，未知异常 SVG 继续按覆盖不完整处理

## 13. 1.1.2 — 2026-08-30

- 过滤嵌入普通字段名的 IPv6 片段和依赖版本误报
- 把标准文档网段、明确合成测试值和 GitHub 公开匿名署名分为可见的非关键风险
- 保留未标记测试凭据、真实私人网络、私人身份和法律记录的发布阻断
- 增加对应的误报与真实泄漏对照测试

## 14. 1.1.1 — 2026-08-29

- 支持受限解析单文件 gzip、bzip2 和 xz 流
- 压缩 tar 继续执行成员数量、展开体积、路径和递归限制
- 修复合法 `.gz` 数据被错误标记为 `invalid-tar` 的兼容问题
- 补齐源代码凭据引用与合成签名网址的非关键分类，凭据字面量继续阻断

## 15. 1.1.0 — 2026-08-29

### 15.1. 稳定接口

- 固定严格审计与宽松发布双结论模型
- 默认发布档位保持 `permissive-noncritical`
- 保留第 3 版私有策略和第 1、2 版内存迁移
- 增加 `--version` 并返回 `github-safe-publish 1.1.0`

### 15.2. 覆盖与恢复

- 为工作树、Git 历史、图片和 PDF OCR 增加有限时、跨进程恢复检查点
- 把私有发现改为无损分页并校验页数、记录数和内容摘要
- 保留 Gitleaks `v8.30.1` 校验和验证与运行时凭据金丝雀
- 把源代码凭据引用与测试签名网址归入固定非关键规则，凭据字面量仍然阻断
- 使用内容摘要绑定工作树对象批准，文件变化后旧批准自动失效
- 排除 SVG 路径坐标和 PowerShell 静态成员语法造成的私人值误报

### 15.3. 运行环境与治理

- 更新 Pillow、NumPy、OpenCV、pypdf 和 PyMuPDF 的锁定版本
- Windows 和 Linux 分别安装对应 OpenCV 包，避免重复实现冲突
- 增加 MIT 许可证、安全政策、贡献指南、问题模板和 CodeQL 工作流
- 复用工作流只接收 `tool_ref` 和 `shadow`，不接收私有策略或 GitHub Actions Secret

## 16. 1.0.0 — 2026-08-27

- 首次公开统一脱敏、严格审计和精确发布检查
- 增加合成测试语料与 3 个试点影子工作流
